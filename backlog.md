# SmartPMO.ai — Product Backlog

**Created:** 2026-02-19
**Last Updated:** 2026-03-05 (session 7) SAST

---

## Priority Key
- 🔴 **HIGH** — Required for launch or critical to product value
- 🟡 **MEDIUM** — Important but not blocking launch
- 🟢 **LOW** — Nice-to-have, defer post-launch

---

| # | Priority | Feature | Description | Added |
|---|----------|---------|-------------|-------|
| B-01 | ✅ DONE | About page | About/Mission page at `/about`. Covers: The Problem (8.5M articles stat with cited sources), Who Built This (career path, 15yr PMO + AI practitioner), What You Get (no vendors/ads/affiliates), How It Works (6-step pipeline visual, high-level only), Three Article Categories (PMO Related/Potential/AI General), Why Share This, CTA to blog + homepage. Dark theme matching index.html. About link added to both desktop and mobile nav. **Spec used:** Direct build from `smartpmo-mission.md` + `website content/ABOUT PAGE.docx` content. **Files:** `website/about.html` (new), `website/index.html` (nav updated). | 2026-02-19 |
| B-02 | ✅ DONE | Newsletter operational | Weekly newsletter LIVE. First send 2026-02-28 (3 subscribers, 20 articles). **Sending:** Resend.com API with DKIM/SPF-verified domain (`newsletter@smartpmo.ai`). Subscriber list via EmailOctopus v2 API. **Card design:** Dark theme, category gradient badges, read time, PMO area tags, clickable titles, TLDR bullets, "Read Article →" links, all-cyan SmartPMO.ai header. Per-subscriber unsubscribe links + `List-Unsubscribe` headers. `--dry-run` flag. Logs to `weekly_newsletters` DB table. **History:** EmailOctopus campaign API dead (v1.6 405, v2 no POST). Gmail SMTP went to spam (no DKIM alignment). Resend.com solved both. **Remaining:** Create Task Scheduler job for `run-newsletter.bat` (Fridays 07:45 SAST). **Files:** `Automation/newsletter/send-weekly-newsletter.js`, `Automation/newsletter/smtp-send.js` (legacy backup), `Automation/newsletter/run-newsletter.bat`. | 2026-02-23 |
| B-03 | ✅ DONE | SEO basics | Gold-standard SEO implemented: optimised title/description, full OG/Twitter tags with dimensions, Organization + WebSite + CollectionPage JSON-LD, robots.txt with AI crawler allowlist, sitemap.xml with lastmod, webmanifest, favicon chain, performance hints. | 2026-02-19 |
| B-04 | ✅ DONE | Voting/usefulness buttons | Thumbs up/down on each card, calls Cloudflare Worker at `smartpmo-votes.ggpera.workers.dev`. Counts display live. **Spec used:** Implemented directly in `website/index.html` (VotesModule IIFE) + `cloudflare-votes-worker/`. | 2026-02-19 |
| B-05 | ✅ DONE | Full AI-PMO assessment | Replace hero 30-second mini-assessment with 15-question assessment on /assessment page. Hero redesigned as Layout B (stacked narrative). Drop mini-assessment entirely. **Specs used:** `SPEC-HERO-REDESIGN-LAYOUT-B.md`, `SPEC-ASSESSMENT-SCORING-AND-QUADRANTS.md`, `SPEC-ASSESSMENT-PAGE.md`. | 2026-02-19 |
| B-09 | ✅ DONE | Provider health auto-detection | AIRouter.js: tracks consecutive failures per provider. At 5 failures fires `⚠️ PROVIDER ALERT` to console + `provider-alerts.log`. Resets on success. `getProviderAlertState()` exposed for health checks. | 2026-02-19 |
| B-10 | ✅ DONE | Auto-open log on pipeline run | run-daily.bat + run-daily-NO-VPN.bat: clear pipeline-output.log, spawn PowerShell live-tail window (Get-Content -Wait), then redirect all pipeline output to pipeline-output.log. Both scheduled and manual runs covered. | 2026-02-19 |
| B-11 | ✅ DONE | Manual vote seeding via console | **Backend:** `POST /api/admin/votes/seed` endpoint. Accepts `{ articleId, upvotes, downvotes }`. Inserts synthetic voter hashes (`seed_up_ID_N`) into Cloudflare D1 via `wrangler d1 execute --remote`. `GET /api/admin/votes/:articleId` reads live counts from CF worker. **UI (v2):** Dedicated "Votes" tab in console. Loads all 20 displayed articles with current vote counts from Cloudflare. Table shows: #, title (linked), category badge, quality score, current up/down counts, seed up/down input fields. "Update All" button sends sequential POSTs for articles with non-zero seeds (avoids wrangler conflicts). Progress indicator ("Seeding 3/5..."). Auto-refreshes counts after seeding. `GET /api/pipeline/displayed-articles` endpoint added (queries `daily_insights WHERE is_displayed = 1`). **Seed overwrite fix (v3):** Changed voter hash from `seed_up_{id}_{i}` to `seed_up_{id}_{timestamp}_{i}` — `INSERT OR IGNORE` with deterministic hashes silently skipped re-seeds; timestamp makes every batch unique and fully additive. **Files:** `01-Management-Console/public/server.js`, `01-Management-Console/public/console.html`, `01-Management-Console/public/console-modules/votes-tab.js` (new). | 2026-02-19 |
| B-06 | 🟡 MEDIUM | Article archive page | Show previous days' cards at `/archive`. Data in `daily_insights` table. Yesterday's 20 articles currently vanish — waste of curated content. | 2026-02-19 |
| B-07 | 🟡 MEDIUM | Category filtering on main page | Let visitors filter displayed cards by PMO_RELATED / PMO_POTENTIAL / AI_GENERAL. Currently all 20 cards look identical in presentation. | 2026-02-19 |
| B-08 | 🟢 LOW | Parallel RSS processing | Process 5-10 RSS feeds concurrently instead of sequentially. Would cut RSS phase from ~25 min to ~5 min. Current 22 min is acceptable. | 2026-02-19 |
| B-12 | ✅ DONE | Discovery schedule audit | Verified: Task Scheduler fires daily at 6:00 AM SAST. History confirms consecutive runs Feb 20–24 (all completed, 14–25 min each). Feb 24: 200 articles discovered, 193 scored. `daily_runs` table confirms 5 consecutive days of full pipeline runs. Minor bug: `daily_runs` uses one row per date — manual re-runs overwrite scheduler data (cosmetic, not blocking). **Spec used:** Direct verification (Task Scheduler screenshot + DB queries). | 2026-02-24 |
| B-13 | ✅ DONE | Share buttons on strategic insight cards | Share buttons (LinkedIn, X, email, copy link, WhatsApp) added to strategic insight cards via `createArticleCard()` function in `website/index.html`. Uses same footer pattern as Latest Intelligence cards (`card-footer-a` with `footer-votes` + `footer-share` layout). **Spec used:** Direct implementation in `website/index.html`. | 2026-02-21 |
| B-14 | ✅ DONE | Replace Weekly Top 20 / Quarterly Report emoji icons with custom SVGs | Generic emoji icons replaced with inline SVGs: envelope with diamond sparkle (Weekly Top 20) and bar chart with trend line (Quarterly Report). Both use brand colours (#00D4FF, #00F5FF, #4C78FF, #825AFF). **Files:** `website/index.html`. | 2026-02-28 |
| B-15 | ✅ DONE | Vote persistence broken — votes reset every session | **Root cause:** `voterHash()` in Worker included `+ day` in the hash seed, rotating the fingerprint daily. Worker treated returning users as new each day. **Fix:** Removed `day` from hash — now `SHA-256(IP + UA)` only, stable across sessions. **Spec used:** Direct fix in `cloudflare-votes-worker/src/index.js`. **Deploy:** `npx wrangler deploy` from worker dir. | 2026-02-21 |
| B-16 | ✅ DONE | Console consolidation — absorb port 3333 into port 8080 | Absorbed port 3333 pipeline runner into port 8080 management console. Pipeline Runner tab with WebSocket live log, Run/Stop, 7-stage progress, cards preview. Scheduler auto-console on `--trigger=scheduler`. Port 3333 retired. **Spec used:** `SPEC-B16-CONSOLE-CONSOLIDATION.md`. | 2026-02-24 |
| B-21 | ✅ DONE | PMO Prompt Prêt-à-Porter page | **ALL 3 TIERS COMPLETE.** Page at `/prompts`. (1) **Tier 1 — Forge:** 4 prompt-building frameworks (CRAFT, GROUNDED, CHAIN, VERIFY) with per-section guidance, live preview, copy + 10 model launchers. Deployed `582fb8d`. (2) **Tier 2 — Prêt-à-Porter:** 22 ready-to-use prompts across 11 PMBOK areas, all with 3+ anti-hallucination techniques, input fields, live preview, copy/launcher, community voting (D1). (3) **Tier 3 — Chains:** 6 multi-step workflows (Full Risk Assessment 5 steps, Project Kickoff Package 5, Scope Validation 4, Sprint Health Check 4, Vendor Selection Pipeline 4, PMO Maturity Diagnostic 4) with step progress bar, paste-previous-output flow, live preview, copy + 10 model launchers. Deployed `63840be`. All tabs active, zero console errors, mobile responsive. **Spec:** `SPEC-B21-PMO-PROMPT-SERVICE.md`. | 2026-02-22 |
| B-22 | 🟡 MEDIUM | There Be Dragons page | New page. Concept TBC — to be workshopped. | 2026-02-22 |
| B-20 | 🔴 HIGH | Blog section | Build blog page at `/blog`. Founder insights, PMO-AI implementation experiences, and system-building lessons. Linked from header nav, About page CTA, and footer. Needs: page template matching dark theme, post listing, individual post rendering. Content authored manually. | 2026-02-22 |
| B-18 | ✅ DONE | Fix Strategic Insight card bullet point rendering | **Problem:** Curated articles displayed summary as unformatted text blob — `<div class="card-summary">` ignored `\n` newlines. **Fix:** Replaced `<div class="card-summary">` with `<ul class="card-tldr">` — same markup as Latest Intelligence cards. Summary split on `\n`, bullet prefixes (`•`, `-`, `*`) stripped via regex, each line rendered as `<li>` with `escapeHtml()`. Reuses existing `card-tldr` CSS (styled bullets, spacing, font). No CSS changes needed. **Files:** `website/index.html` — `createArticleCard()` function. | 2026-02-22 |
| B-19 | ✅ DONE | Make all fields editable in Curated Articles Manager | Replaced single `prompt()` dialog with full edit modal. All fields editable: title, URL, summary, source name, published date, display order, is_active (Active/Inactive dropdown), notes. Modal pre-populates from API on edit click. Uses existing `PUT /api/admin/curated-articles/:id` backend (all fields already in allowedFields whitelist). Close on overlay click or Cancel button. **Files:** `01-Management-Console/public/curated-articles-manager.html`. | 2026-02-22 |
| B-24 | ✅ DONE | Groq fallback broken — replaced with OpenRouter | Groq geo-blocks South Africa (403). Groq disabled in DB, VPN logic removed from `run-daily.bat`. OpenRouter added as priority 4 scoring fallback (`meta-llama/llama-3.3-70b-instruct:free`, 50/day, no geo-blocks). `case 'openrouter':` in AIProviderAdapter.js. API key verified. New cascade: cerebras(1) → fireworks(3) → openrouter(4) → openai(6). **Spec used:** `SPEC-B24-OPENROUTER-PROVIDER.md`. | 2026-02-24 |
| B-25 | ✅ DONE | Scoring JSON parse failure — switch to text output | Run 177: 73/119 articles (61%) failed scoring. Fixed: replaced JSON output with text markers (CATEGORY:/SCORE: + REASONING:). 3-strategy fallback chain (text → JSON → bare keyword). Classification v1.2 (id=16), Quality v1.2 (id=17). v1.1 deactivated. max_tokens bumped 200→500. **Verified:** 33/33 scored, 0 parse failures. **Spec used:** `SPEC-B25-TEXT-SCORING-OUTPUT.md`. | 2026-02-24 |
| B-26 | ✅ DONE | Newsletter sender email + quarterly report automation | Cloudflare Email Routing configured (`newsletter@smartpmo.ai` → `smartpmoai@gmail.com`). EmailOctopus DNS verified (5 records: DKIM, DMARC, 3 CNAMEs). Domain `smartpmo.ai` verified. Quarterly report automation created: trigger (contact added to "Quarterly Report" list) → send email ("Your Report is Ready" + "Download Your Report" button → `smartpmo.ai/reports/SmartPMO-AI-Maturity-Q4-2025.pdf`). Form ID embedded in index.html. PDF hosted. Website deployed. **Newsletter sender unblocked** — first live send of B-02 now possible. **Email template refined:** dark theme applied (bg `#0A0A0F`, font `#E0E0E0`, links `#00D4FF`), social icons (Facebook/X/Instagram) removed, sender address updated to fictitious UK address (privacy). Automation reactivated. **Spec used:** Direct implementation via Cloudflare dashboard + EmailOctopus dashboard + browser automation. **Files:** `website/index.html` (form ID + status text), `website/reports/SmartPMO-AI-Maturity-Q4-2025.pdf` (new). | 2026-02-27 |
| B-31 | ✅ DONE | Investigate and remediate Google CSE | **Root cause:** Three compounding bugs. (1) `qdr:d` web search operator baked into all 62 query strings — not supported by CSE JSON API, either silently ignored or treated as literal text. (2) `after:/before:` date operators appended in code — also web search syntax, not CSE API params. (3) No daily query counter — 62 queries/run × 2+ runs/day exceeded 100/day free tier, causing 429 rate limits on 40% of runs. **Fix:** (1) Stripped `qdr:d` from all 62 query strings in `source_registry` DB. (2) Replaced `after:/before:` code with proper `dateRestrict: 'd2'` CSE API parameter (last 48h). (3) Added daily query counter with 90-query cap (reserves 10 for manual runs), logs usage, skips gracefully when limit reached. **Verified:** Test query returned 270,000 results and 10 items (was returning 0). Inference queries also working (13,800 results). **Files:** `Automation/modules/DiscoveryEngine.js` (discoverGoogle method rewritten), `pmo_insights.db` (62 source_registry rows updated). | 2026-02-27 |
| B-27 | ✅ DONE | Blog source expansion (55 RSS feeds) | 55 new RSS sources added to `source_registry` from AI×PMO discovery research (15 Tier 1, 28 Tier 2, 12 Tier 4). RSS validation performed, INSERT into source_registry, smoke tested. Category A (intersection) + Category B (PMO_POTENTIAL). Committed `0e63138`. **Spec:** `SPEC-B27-BLOG-SOURCE-EXPANSION.md`. | 2026-02-24 |
| B-28 | 🟡 MEDIUM | No-RSS blog ingestion (~97 sources) | ~97 blogs from AI×PMO discovery report lack RSS feeds. Requires new ingestion method: sitemap polling, web scraping, or newsletter-to-RSS conversion. Sources list in `AI × PMO blog discovery 166.txt`. Depends on B-27 completion first. | 2026-02-24 |
| B-29 | 🟡 MEDIUM | Manual pipeline runs don't trigger Discord alert | `alertManager.pipelineSuccess()` fires in `run-daily-pipeline.js` but manual runs via `node run-daily-pipeline.js` don't send Discord webhook. Investigate whether it's a code path issue or webhook config. Discovered: Run 184 manual run completed successfully but no Discord notification received. | 2026-02-27 |
| B-30 | ✅ DONE | Gemini markdown fence stripping in ContentEnricher | **Problem:** Gemini wraps JSON responses in markdown fences (` ```json...``` `) ~26% of the time, causing "No JSON found in response" enrichment failures. ContentEnricher's regex `/{[\s\S]*}/` didn't strip fences. ScoringEngine already had the fix. **Fix:** Ported robust `parseJSON` pattern from ScoringEngine: try markdown fence extraction first, fall back to raw JSON, clean trailing commas + control chars. **Files:** `Automation/modules/ContentEnricher.js` (lines 351-377). | 2026-02-27 |
| B-58 | ✅ DONE | Gemini enrichment returns plain text instead of JSON (~27% failure) | **Problem:** B-30 only fixed fence-wrapped JSON. Gemini still returned plain text (zero braces) ~27% of the time (8/30 in Run 2026-03-05). Root cause: `geminiComplete()` in AIProviderAdapter.js never requested JSON mode — Gemini had no constraint to return JSON. **Fix:** (1) Added `responseMimeType: "application/json"` to `generationConfig` in `AIProviderAdapter.geminiComplete()` — forces Gemini to always return valid JSON. (2) Made ContentEnricher fence regex case-insensitive + any-language-tag as safety net. (3) Added raw response logging (first 300 chars) on parse failure for future debugging. **Files:** `Automation/modules/AIProviderAdapter.js` (line 251), `Automation/modules/ContentEnricher.js` (lines 358-370). | 2026-03-05 |
| B-32 | ✅ DONE | Subscription form UX overhaul | **Multiple fixes:** (1) Subscribe buttons: replaced broken CSS variable gradient with working hex colours (#06B6D4→#825AFF), added visible border, black button text. (2) Form sizing: both forms matched at 400px max-width. (3) Success messages: fixed `p:first-child` CSS rule hiding messages, increased specificity to override EmailOctopus Bootstrap styles, green (#4ade80) on dark background. (4) Custom SVG icons (see B-14). (5) Mobile: full-width forms, adjusted input/button sizing in `@media (max-width: 768px)`. (6) Duplicate subscription guard: v3 had race condition (click handler saved to localStorage → submit handler blocked it → all subscriptions broken). **v4 fix:** removed `saveSub()` from click+submit handlers (block-only), added MutationObserver that saves to localStorage only on confirmed EmailOctopus success message. Yellow "already subscribed" (#facc15) on re-submit. **Files:** `website/index.html`, `website/styles-v2.css`, `website/hero.css`. **EmailOctopus:** Quarterly Report success message updated, double opt-in enabled on Weekly Top 20, welcome email refined (dark theme, no social icons). | 2026-02-28 |
| B-23 | ✅ DONE | EmailOctopus subscriber count monitor | **Backend:** `GET /api/admin/subscribers` endpoint: fetches from EmailOctopus v1.6 API (`api_key` query param), 1-hour server-side cache. Returns total count, per-list breakdown (subscribed + unsubscribed), alert level (green/amber at 2,000+/red at 2,300+), and free plan limit (2,500). **Header pill:** "SUBS: 11" pill in console hero bar (always visible, all tabs). Green/amber/red colour coding. **Subscribers tab (v2):** Dedicated tab with full dashboard. 4 summary cards (Total Subscribed, Total Unsubscribed, Free Plan Limit, Status). Progress bar showing usage toward 2,500 limit. Breakdown table by list (Weekly Top 20 / Quarterly Report) with subscribed, unsubscribed, and net columns. Totals row. Last-fetched timestamp with cache note. Refresh button. **Also fixed:** Added `dotenv` loading to server.js (was missing — key never read from `.env`). Fixed API URL from v2.0 Bearer auth to v1.6 `api_key` param. **Files:** `01-Management-Console/public/server.js`, `01-Management-Console/public/console.html`, `01-Management-Console/public/console-modules/subscribers-tab.js` (new). | 2026-02-23 |
| B-33 | ✅ DONE | Voting buttons fixed — race condition + cache headers | **Root cause:** `VotesModule.init()` only watched `auto-articles-grid` with MutationObserver. Strategic Insights (`curated-articles-grid`) rendered after `loadAllVotes()` fired — curated card votes never loaded, always showing 0/0 with `aria-pressed="false"` (allowing re-votes). **Fix:** (1) Observer now watches BOTH grids via `GRID_IDS` array. (2) `loadedArticleIds` Set prevents duplicate API calls when second grid triggers. (3) Health check result cached (`apiHealthy` flag) — no redundant `/health` probes. (4) Worker responses now include `Cache-Control: no-store, no-cache, must-revalidate`. **D1 confirmed healthy:** article 68054 = 9 upvotes persisted, `UNIQUE(article_id, voter_hash)` dedup working. **Files:** `website/index.html` (VotesModule), `cloudflare-votes-worker/src/index.js`. **Deployed:** Worker v61f5e796 + Pages. | 2026-02-28 |
| B-34 | 🟡 MEDIUM | TLDR first bullet wrapped in bold markers | **Problem:** First bullet point on Latest Intelligence cards is wrapped in `**text**` — the markdown bold markers are showing as literal asterisks instead of being stripped. **Root cause:** `parseTldr()` or the enrichment prompt returns first bullet with `**bold**` markdown that isn't being cleaned. **Fix:** Strip `**` wrappers from TLDR bullets in `buildCardHTML()` (newsletter) and card rendering in `index.html`. **Files:** `website/index.html`, `Automation/newsletter/send-weekly-newsletter.js`. | 2026-02-28 |
| B-40 | ✅ DONE | Console Pipeline Runner crashes server on spawn | **Root cause (v1 misdiagnosis):** Initially attributed to missing error handlers. v1 fix added try/catch wrappers but server still crashed within 0.8s of spawn — 300+ "Disconnected. Reconnecting in 3s..." messages over 40 min. **Root cause (v2):** IPC named pipes on Windows Node.js v22.17.0 crash the server process in a way JS error handlers cannot catch. Additionally, `ErrorHandler.js` has module-level `process.on('uncaughtException')` handlers that call `process.exit(1)` — loaded via analytics routes import chain, overriding server's own handlers. **Fix (v2):** (1) Removed IPC from spawn entirely (`stdio: ['ignore', 'pipe', 'pipe']`). (2) Added STDOUT_JSON transport to PipelineEvents — child writes JSON lines to stdout, server parses and broadcasts via WebSocket. (3) Line-buffered stdout parsing (`lines.pop()` keeps incomplete lines). (4) Guarded ErrorHandler.js module-level handlers to only register for pipeline scripts (not server). (5) Added `process.removeAllListeners('uncaughtException')` + safe re-registration at end of server.js. (6) Added `process.on('exit')` diagnostic handler. **Verified:** Server survives pipeline spawn, stop button works, JSON events flow correctly. **Files:** `01-Management-Console/public/server.js`, `Automation/lib/PipelineEvents.js`, `Automation/run-manual-console.js`, `Automation/modules/ErrorHandler.js`. | 2026-03-01 |
| B-41 | ✅ DONE | Console Pipeline Runner Stop button non-functional | **Root cause:** `currentPipeline.kill('SIGTERM')` doesn't kill child process trees on Windows. **Fix:** Windows path uses `taskkill /pid ${pid} /T /F` (tree-kill, force) with fallback to `SIGKILL`. Unix path retains `SIGTERM` with 5-second timeout → `SIGKILL` force-kill. Stop response now includes PID for diagnostics. **Files:** `01-Management-Console/public/server.js`. | 2026-03-01 |
| B-17 | ✅ DONE | "Deploy to Live" button in Curated Articles Manager | `POST /api/admin/deploy` endpoint: regenerates `displayed-articles.json`, then execs `push-bust.ps1` (git add, commit, cache-bust CSS, push). Returns git SHA on success. "Deploy to Live" button added to manager toolbar with confirmation dialog + loading state + success/error toast. 60s timeout. Not auto-triggered on save (explicit control only). **Files:** `01-Management-Console/public/server.js`, `01-Management-Console/public/curated-articles-manager.html`. | 2026-02-22 |
| B-35 | ✅ DONE | Add SRI hashes to CDN scripts | **Problem:** Lucide loaded from unpkg CDN without Subresource Integrity (SRI) hash. If CDN is compromised, malicious JS executes on smartpmo.ai. **Fix:** Added `integrity="sha384-GzE8qT1LzDfRNjtvLN/dXDLPlJOB3BBom3Twb9/RwHkXCgNl6OU1pBxM9397HWGU"` + `crossorigin="anonymous"` to pinned `lucide@0.575.0/dist/umd/lucide.min.js` script tag. **Files:** `website/index.html`. | 2026-02-28 |
| B-36 | ✅ DONE | IIFE-wrap card rendering module | **Problem:** Design contract §1/§3 requires all JS modules wrapped in IIFE. Card rendering functions (~15 functions, 26KB) were all global scope. **Fix:** Wrapped in `(function() { 'use strict'; ... })();`. 5 share functions exposed via `window.shareToLinkedIn` etc. (required by inline onclick attributes). All internal functions and config objects now private scope. All 4 JS script blocks validated with `new Function()` — zero parse errors. **Files:** `website/index.html`. | 2026-02-28 |
| B-48 | ✅ DONE | Website load speed — assessment + homepage | **Problem:** Assessment page felt slow. Root cause: 37KB JS parser-blocking (no `defer`), no resource preloading, Google Fonts 3-hop render chain. **Profiling:** curl timing showed 655ms parser-blocking JS, 431ms fonts CSS chain, 550ms TTFB. **Fixes applied:** (1) Assessment: added `defer` to `section-assessment-page.js` script tag (eliminates 655ms parser block). (2) Assessment: added `<link rel="preload">` for JS (overlaps download with CSS parsing, saves ~200ms). (3) Homepage: added `<link rel="preload">` for Lucide CDN (383KB — starts download before parser reaches script tag). (4) Assessment footer copyright updated to 2026. Font weight trim investigated but 800/900 confirmed in use by assessment CSS — not safe to remove. **Files:** `website/assessment/index.html`, `website/index.html`. | 2026-02-28 |
| B-37 | ✅ DONE | Inline onclick → event delegation | All 10 share button `onclick` handlers (5 auto + 5 curated) replaced with `data-share-action` + `data-share-payload` attributes. Single delegated listener on `document` handles all share actions. `window.*` exports removed (no longer needed). `copyLink` correctly receives button ref via `{ currentTarget: btn }`. Static onclicks (HeaderMenu, HeroAssessment) left as-is — no dynamic data, no XSS risk. **Files:** `website/index.html`. | 2026-02-28 |
| B-38 | ✅ DONE | CSS !important audit and reduction | Full audit: 217→187 declarations (30 removed). Removed 30 duplicate `.article-card .shr-icon.btn-*:hover` rules with `!important` from `section-cards.css` (exact copies of existing rules at higher specificity — unnecessary). Remaining 187 all justified: 82 EmailOctopus third-party overrides, 93 mobile-fixes.css responsive overrides (architectural pattern), 4 accessibility (prefers-reduced-motion), 1 display:none. **Files:** `website/section-cards.css`. | 2026-02-28 |
| B-39 | ✅ DONE | Add skip-to-content link (WCAG 2.1 AA) | Skip link added as first child of `<body>`, hidden off-screen via `top: -100%`, slides to `top: 0` on `:focus`. Targets `id="main-content"` on hero section. Styled in cyan brand color, z-index 10000. CSS in `styles-core.css` (accessibility section). **Files:** `website/index.html`, `website/styles-core.css`. | 2026-02-28 |
| B-40 | 🟡 MEDIUM | Custom 404 page | **Problem:** No custom 404 page. Cloudflare Pages shows default "404 Not Found" when users hit a dead URL. Missed branding + navigation opportunity. **Fix:** Create `website/404.html` matching dark theme. Include nav header, "Page not found" message, link back to homepage + search suggestion. Cloudflare Pages automatically serves `404.html` from root. **Risk:** Zero — new file, no existing functionality affected. **Files:** `website/404.html` (new). | 2026-02-28 |
| B-41 | ✅ DONE | CSS code-splitting — extract section stylesheets | Monolithic `styles-v2.css` split: 66KB→13KB. Removed 2,900 lines of dead CSS (old header, old hero, old quiz, progressive reveal — all replaced by hero.css, header-styles.css, assessment page). Extracted 981 lines of card styles to new `section-cards.css`. Further cleanup removed 209 more dead lines from section-cards.css (old mobile menu, hamburger, legacy classes). **Files:** `website/styles-v2.css`, `website/section-cards.css` (new), `website/index.html`. | 2026-02-28 |
| B-42 | ✅ DONE | Global element selectors → scoped classes | Bare `footer {}` selector replaced with `.site-footer {}`, class added to HTML. Removed dead `.nav a` and `.btn-primary` inline styles (classes don't exist in HTML). Removed dead `.mobile-menu` block (93 lines), `.hamburger-btn`/`.hamburger-line` (27 lines), `.linkedin-primary`, `.pmo-direct`, `.pmo-inferred` legacy classes, dead icon-color classes. Total: 209 lines dead CSS removed from section-cards.css. **Files:** `website/styles-v2.css`, `website/section-cards.css`, `website/index.html`. | 2026-02-28 |
| B-43 | ✅ DONE | Remove unused font weights (Inter 800/900) | **Audit result:** Both 800 and 900 actively used. 800: hero.css (3x), header-styles.css, assessment pages (3x), styles-v2.css. 900: hero.css (2x), assessment page (2x). Cannot remove — all weights needed. However, weight 300 is unused — trimmed `Inter:wght@300..900` to `Inter:wght@400..900`, saving one weight variant (~15KB). **Files:** `website/index.html`. | 2026-02-28 |
| B-44 | ✅ DONE | Add image lazy loading + srcset | **Audit result:** No `<img>` tags exist on the page — all content is JS-rendered. Hero-barista images only referenced in `<link rel="preload" imagesrcset="...">` which already provides responsive selection (600w/1200w). No further optimization possible without adding WebP variants (requires image conversion tooling). **Files:** N/A — already optimized. | 2026-02-28 |
| B-45 | ✅ DONE | Service worker for offline caching | Created `sw.js`: cache-first for static assets (CSS, images, fonts, CDN), network-first for API/JSON with cache fallback. Pre-caches 12 critical assets on install. Version-based cache names (`smartpmo-v1`) for clean invalidation on deploy. Registered at end of `<body>`, silent failure if unsupported. **Files:** `website/sw.js` (new), `website/index.html`. | 2026-02-28 |
| B-46 | ✅ DONE | Clean up duplicate CSS variables | **Audit result:** `--color-cyan` was only defined in old styles-core.css (removed in B-47 rewrite) and used in unlinked dead files (styles-v3.css, ux-overhaul-styles.css, mobile-menu-styles.css). All linked CSS files use `--cyan-primary` exclusively. No duplicates remain. Single clean variable set in styles-v2.css `:root`. **Files:** N/A — resolved by B-47 cleanup. | 2026-02-28 |
| B-47 | ✅ DONE | Remove or link dead styles-core.css | Deduplicated and linked. Removed duplicate reset, body, root variables (already in styles-v2.css). Removed dead class refs (quiz-option-content, btn-next, btn-back, card-hover). Kept unique useful rules: skip-link (B-39), focus-visible, sr-only, prefers-reduced-motion, GPU acceleration hints, below-fold content-visibility. Linked as first stylesheet in chain. **Files:** `website/styles-core.css`, `website/index.html`. | 2026-02-28 |
| B-52 | 🟡 MEDIUM | Nav anchor links jump to cards, not section headings | **Problem:** Clicking "Latest Intelligence" or "Strategic Insights" in the header nav scrolls to the first row of cards, skipping the section heading. User expects to land at the section title. **Fix:** Move the anchor `id` (or add a new one) to the section heading element (e.g. `<h2>`) or a wrapper div above the cards grid, not on the grid itself. Apply `scroll-margin-top` to account for the sticky header height. **Files:** `website/index.html`. | 2026-02-28 |
| B-51 | 🟡 MEDIUM | About page "View Today's Insights" links to wrong anchor | **Problem:** "View Today's Insights" link at the bottom of `/about` navigates to `smartpmo.ai` (top of homepage). Should jump directly to the Latest Intelligence section. **Fix:** Change href from `/` (or `index.html`) to `/#latest-intelligence` (or whichever `id` the section uses). Verify the target section has the correct `id` attribute on homepage. **Files:** `website/about.html`, possibly `website/index.html` (if section `id` is missing). | 2026-02-28 |
| B-50 | ✅ DONE | Share links must attribute SmartPMO.ai | **Implemented:** Share landing pages at `website/r/{id}.html` with SmartPMO.ai branding, OG meta tags, and auto-redirect to source article. Article archive at `website/api/article-archive.json`. SharePageGenerator added as pipeline Step 6.7 (generates landing pages + archive on each run). 5 share icons per card: LinkedIn, X, Email, Copy, WhatsApp — all share the SmartPMO.ai landing page URL (not raw article URL). Instagram button removed (no share URL API exists — was non-functional on both mobile and desktop). Event delegation pattern (B-37) handles all share clicks. CSS `fill` fix for SVG icon color inheritance on mobile WebKit. **Files:** `website/index.html`, `website/section-cards.css`, `Automation/modules/SharePageGenerator.js`, `Specs/SPEC-B50-SHARE-ATTRIBUTION.md`. | 2026-03-01 |
| B-53 | ✅ DONE | Mobile card footer layout broken — votes/date/share icons crushed | **Problem:** On mobile, the card footer crams vote buttons (thumbs up/down with counts), published date, and 5 share icons (LinkedIn, X, email, copy, WhatsApp) into a single row. Vote counts overlap/stack (e.g. "45" wrapping to next line inside the button), date is truncated ("26 Feb..." or "26 F..."), share icons are squashed. **Fix:** CSS-only in `mobile-fixes.css`. Global: `flex-wrap: wrap` + `row-gap: 6px` on `.card-footer-a`, `flex: 1 1 auto` on footer-left/footer-votes, `flex-shrink: 0` on footer-right/footer-share. Mobile-only (`@media max-width: 767px`): `width: 100%` on both footer-left and footer-right (forces two-row layout — Row 1: votes + date, Row 2: share icons). Larger touch targets (30px share icons, 30px vote buttons). Date font bumped to 0.75rem with `overflow: visible`. Desktop layout unchanged (rules scoped to ≤767px). **Files:** `website/mobile-fixes.css`. | 2026-03-01 |
| B-54 | 🟡 MEDIUM | Strategic Insights card footer — remove source, align date+votes to one line | **Problem:** Strategic Insights cards show a "source" line in the footer that isn't needed. Date and voting buttons should sit on a single line matching the Latest Intelligence card footer layout. **Fix:** Remove source display from Strategic Insights card footer. Ensure date and vote buttons render on one row (same layout as Latest Intelligence cards). **Files:** `website/index.html` (card rendering JS — `createArticleCard()` or curated card builder). | 2026-03-01 |
| B-55 | 🟡 MEDIUM | Instagram auto-posting (top articles) | **Concept:** Create @SmartPMO.ai Instagram Business account. Auto-post top 1-2 daily articles as branded card images (title + category badge + SmartPMO.ai branding on dark template). Caption: TLDR + hashtags + "Link in bio → smartpmo.ai". **Requires:** Instagram Business account + Facebook Page, Meta Graph API (Content Publishing API), image generation (canvas/sharp in Node), new pipeline step after scoring, Meta app review for publishing permissions. **Effort:** ~3-4 hours build + Meta review process. **Related:** Instagram share button removed from cards (no share URL API exists — button was non-functional). Teams share button considered but deferred (requires external `launcher.js` dependency, desktop-only). | 2026-03-01 |
| B-49 | ✅ DONE | Hero quadrant axis positioning (desktop + mobile) | **Problem:** X-axis ("LOW — AI ADOPTION — HIGH") overlapped the quadrant grid on desktop. Y-axis ("LOW VALUE HIGH") completely missing on mobile — hidden via `display: none` on SVGs, tiny 8-9px fonts. Service worker (B-45) cache-first strategy blocked all CSS/HTML updates on mobile browsers. **Fix:** (1) Moved x-axis div from inside `.hero-quadrant-grid` to after it in `.hero-visual-wrapper` (flex-column, normal flow). `margin-top: 40px` gap on desktop. (2) Mobile y-axis: replaced `display: none` SVGs + shrunken fonts with `scale(0.65)` on the full desktop element — keeps gradient lines and labels intact, just proportionally smaller. `left: -150px` (768px), `left: -145px` + `scale(0.55)` (375px). (3) Killed B-45 service worker entirely — `sw.js` now self-destructs (clears caches + unregisters). HTML registers SW kill + cache purge on load. (4) Removed per-section `overflow-x: hidden` from `.hero-section` and `.hero-visual-wrapper` on mobile (was clipping y-axis); `html, body { overflow-x: hidden }` handles page-level overflow. **Files:** `website/hero.css`, `website/mobile-fixes.css`, `website/index.html`, `website/sw.js`. | 2026-02-28 |

| B-56 | 🟡 MEDIUM | Newsletter "Prompt of the Week" integration | Add a "Prompt of the Week" section to the B-02 weekly newsletter featuring a highlighted prompt from `/prompts`. Drives traffic back to the Prompt Service. Requires B-21 Phase 1 completion. | 2026-03-02 |
| B-57 | 🟡 MEDIUM | PMBOK 8 Performance Domain prompts | Create additional prompt set organised by PMBOK 8 Performance Domains (Stakeholders, Team, Development Approach & Life Cycle, Planning, Project Work, Delivery, Measurement, Uncertainty). Supplement the existing PMBOK 6 Knowledge Area taxonomy. Requires B-21 Phase 1 completion. | 2026-03-02 |

---

## Completed / Moved to Fixes
Items that started as backlog but got implemented are tracked in `status.md` Fixes Summary table.
